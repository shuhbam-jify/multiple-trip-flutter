enum AppTheme { SYSTEM, DARK, LIGHT }

enum NavBarEnum { HOME, SEARCH, CREATE, PROFILE }

enum PostCardEnum { DETAILS, WITHOUT_DETAILS }

enum PostCardUser { HOME_PAGE, DETAILS_PAGE }

enum DrawerItemType {
  PLANS_AND_PRICES,
  PAST_QR_CODES,
  TERMS_OF_USE,
  PRIVACY_POLICY
}

enum AppPermissions {
  granted,
  denied,
  restricted,
  permanentlyDenied,
}
